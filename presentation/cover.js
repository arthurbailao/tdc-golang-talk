import React from 'react';

import {Cover} from 'dito-spectacle-theme';

export default () => (
  <Cover
    variant={4}
    title={
      <span>
        Benchmark Go vs Node em arquitetura Serverless
      </span>
    }
    titleSuffix="."
  />
);
